{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the BLE3D main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
